# 다음 Claude에게 전달하는 인수인계 프롬프트

---

나는 온프레미스 서버 CPU 과부하 → AWS 자동 확장 트리거 시연을 위한 테스트용 숙박예약 웹서비스를 만들고 있어.

## 현재까지 완료된 것

**서비스 구성**
- 프론트엔드: React + Vite (전 페이지 백엔드 API 연동 완료)
- 백엔드: FastAPI + asyncpg (전 엔드포인트 구현 완료)
- DB: PostgreSQL 16 (솔데스크 실제 데이터 기반 schema.sql 완성)
- 배포: Docker + Docker Compose

**핵심 메커니즘**
쿠폰 발급(`POST /coupons/issue`)과 예약 생성(`POST /bookings`)에서 PostgreSQL `SELECT FOR UPDATE` 비관적 락으로 DB 락 경합 → CPU 급등 → AWS 자동 확장 트리거

**완성된 파일들**
```
deploy/
├── build-push.sh           Docker Hub(dk12dl) 이미지 푸시 스크립트
├── vm1-app/                앱 서버 (Nginx+React) - install.sh 포함
├── vm2-api/                API 서버 (FastAPI) - install.sh 포함
├── vm3-db/                 DB 서버 - install.sh + schema.sql 포함
└── vm4-loadtest/           부하 서버 - install.sh + run.sh + loadtest/ 포함

loadtest/
├── jmeter/
│   ├── scenario1_coupon_rush.jmx    쿠폰 선착순 (1000명/10초/SOLD-LOAD)
│   └── scenario3_event_open.jmx    복합 플로우 (800명/30초/SOLD-LOAD)
└── scripts/
    ├── create_users.py     테스트 유저 생성
    └── reset_db.py         부하테스트 후 DB 초기화
```

**DB 주요 데이터**
- 숙소: 124개 (서울/제주/부산/완도/광주)
- 솔데스크 숙소 3개: 강남파빌리온점, 강남성옥빌딩점, 종로점 (이벤트 연결됨)
- 이벤트: 솔데스크 전 지점 동시 오픈 기념 (30% 할인)
- 쿠폰: SOLD-0001~0500 (개별) + SOLD-LOAD (부하테스트용 풀 1000장)
- 계정: admin@stays.com / admin1234, user1@test.com / password123

**서버 구성 (VMware Ubuntu 24.04, Windows PC i7-9세대 32GB)**
```
VM1  앱 서버   1코어 1GB  Nginx+React  :80
VM2  API 서버  2코어 4GB  FastAPI      :8000
VM3  DB 서버   2코어 4GB  PostgreSQL   :5432
VM4  부하 서버  2코어 6GB  JMeter       -
```

**이미지**
- 저장 위치: 프로젝트 루트 `images/` 폴더
- 파일명: soldesc_jongno.jpg, soldesc_gangnam_pavilion.jpg, soldesc_gangnam_seongok.jpg
- Nginx에서 `/images/` 경로로 서빙
- 아직 실제 이미지 파일은 미삽입

## 지금 해야 할 일

### 1. VMware VM 4개 세팅
- Ubuntu 24.04 LTS 설치 (각 VM 사양 위 참고)
- VMware Workstation 사용

### 2. 각 VM IP 확인 후 배포

**배포 순서 (VM3 → VM2 → VM1 → VM4)**

```bash
# Windows에서 파일 복사
scp -r deploy\vm3-db\ user@VM3_IP:~/
scp -r deploy\vm2-api\ user@VM2_IP:~/
scp -r deploy\vm1-app\ user@VM1_IP:~/
scp -r deploy\vm4-loadtest\ user@VM4_IP:~/

# VM2 IP 교체 (복사 전에)
# deploy/vm2-api/docker-compose.yml 에서 VM3_IP, VM1_IP 실제 IP로 교체

# Docker Hub 이미지 빌드+푸시 (Windows에서)
# deploy/build-push.sh 에서 VITE_API_URL=http://VM2_IP:8000 으로 수정 후 실행
```

### 3. 이미지 파일 삽입
```
images/soldesc_jongno.jpg
images/soldesc_gangnam_pavilion.jpg
images/soldesc_gangnam_seongok.jpg
→ docker compose up --build -d 로 재빌드
```

### 4. 부하테스트 실행
```bash
# VM4에서
python3 ~/loadtest/scripts/create_users.py --url http://VM2_IP:8000 --count 2000
./run.sh 1 VM2_IP   # 시나리오1: 쿠폰 선착순 폭발
./run.sh 3 VM2_IP   # 시나리오3: 복합 플로우
```

### 5. 모니터링 세팅 (별도 진행)
- Prometheus + Grafana (VM2 CPU 실시간 모니터링)
- CloudWatch (AWS 연동 후)

### 6. AWS 자동 확장 연동 (별도 진행)
- CPU 80% 초과 감지
- AWS EC2 자동 투입
- 트래픽 분산
- 정상화 시 자원 회수

### 7. 시나리오 2, 4 추가 (별도 진행)
- 시나리오2: 특가 숙소 예약 경쟁 (500명)
- 시나리오4: 인기 숙소 집중 조회 (2000명)
- 테마는 미정

## 중요 참고사항

- Docker Hub 계정: dk12dl
- 이미지: dk12dl/stays-backend:latest, dk12dl/stays-frontend:latest
- 쿠폰 부하테스트 방식: SOLD-LOAD 단일 코드로 1000명이 같은 row 경쟁 → FOR UPDATE 락 → CPU 급등
- schema.sql 위치: deploy/vm3-db/schema.sql (이게 실제 버전, backend/schema.sql 아님)
- VM3→VM2 배포 순서 필수 (DB 먼저 떠야 API 서버 정상 기동)
