# 온프레미스 → 클라우드 자동 확장 서비스 설계

## 개요

온프레미스 서버를 운영 중인 기업이 트래픽 폭증 시 AWS로 자동 확장되는 구조를 구성해주는 서비스.
웹폼으로 서버 정보를 입력받아 자동으로 모니터링, VPN, 스테이징, 부하테스트, 확장까지 처리.

---

## 전체 플로우

```
1. 기업이 웹폼에 서버 정보 입력
        ↓
2. SSH 접속 → 서버 현황 자동 수집
        ↓
3. Ansible로 모니터링 에이전트 자동 설치
        ↓
4. VPN 터널 구성 (온프레미스 ↔ AWS)
        ↓
5. 스테이징 서버 구성 (운영 서버 복제)
        ↓
6. 스테이징에 부하테스트 실행
        ↓
7. CloudWatch 경보 → Lambda → EC2 자동 확장
        ↓
8. ALB로 온프레미스 + AWS 트래픽 분산
        ↓
9. 부하 정상화 → EC2 회수
```

---

## STEP 1. 웹폼 설계 — 수집 정보

### 1-1. 서버 기본 정보

```
┌─────────────────────────────────────────┐
│  서버 등록                               │
├─────────────────────────────────────────┤
│ 서버 IP / 도메인  [               ]      │
│ SSH 포트          [22            ]       │
│ OS 종류           [ Ubuntu ▼    ]        │
│                   (Ubuntu / CentOS / RHEL/ Debian)
├─────────────────────────────────────────┤
│  인증 방식                               │
│  ● 비밀번호   ○ PEM 키 업로드            │
│                                         │
│  [비밀번호 선택 시]                       │
│  SSH 유저명   [ubuntu          ]         │
│  비밀번호     [●●●●●●●●       ]         │
│                                         │
│  [PEM 키 선택 시]                        │
│  SSH 유저명   [ec2-user        ]         │
│  PEM 파일     [파일 선택...]              │
├─────────────────────────────────────────┤
│  애플리케이션 정보                        │
│  서비스 포트   [8000           ]         │
│  앱 타입       [ Web API ▼    ]          │
│               (Web API / Frontend / DB)  │
│  Docker 사용   [✅ 예  ○ 아니오]          │
│  컨테이너명    [stays-backend  ]         │
├─────────────────────────────────────────┤
│         [연결 테스트]  [등록하기]          │
└─────────────────────────────────────────┘
```

### 1-2. 보안 고려사항

```
수집 정보 암호화 저장
  - SSH 비밀번호: AES-256 암호화
  - PEM 키: 서버 메모리에만 보관, DB 저장 안 함
  - 연결 테스트 후 자격증명 즉시 파기 옵션

최소 권한 원칙
  - sudo 없이 읽기 전용으로 정보 수집
  - 설치 단계에서만 sudo 권한 요청
```

### 다른 방법: Agent 방식

웹폼 대신 회사 서버에 에이전트를 설치하는 방법.

```bash
# 회사 서버에서 실행 (원라이너)
curl -fsSL https://서비스URL/install.sh | bash -s -- --token 발급토큰
```

```
장점: SSH 정보 노출 없음, 방화벽 안에서 동작
단점: 서버에 직접 설치해야 함
```

---

## STEP 2. SSH 접속 및 서버 정보 수집

### 2-1. 연결 테스트 클릭 시 백엔드 처리

```python
# 백엔드에서 처리
import paramiko

ssh = paramiko.SSHClient()
ssh.set_missing_host_key_policy(paramiko.AutoAddPolicy())
ssh.connect(hostname=ip, port=port, username=user, password=pw)

# 기본 정보 수집
commands = {
    "os_info":     "cat /etc/os-release",
    "cpu_cores":   "nproc",
    "cpu_usage":   "top -bn1 | grep 'Cpu(s)'",
    "memory":      "free -h",
    "disk":        "df -h /",
    "uptime":      "uptime",
    "open_ports":  "ss -tlnp",
    "docker":      "docker ps 2>/dev/null || echo 'no docker'",
    "services":    "systemctl list-units --type=service --state=running",
    "network":     "ip addr show",
    "hostname":    "hostname -f",
}
```

### 2-2. 수집 결과 화면에 표시

```
┌─────────────────────────────────────────────────┐
│  서버 현황                                        │
├────────────┬────────────────────────────────────┤
│ OS         │ Ubuntu 24.04.1 LTS                  │
│ CPU        │ 4코어 / 현재 사용률 23%              │
│ 메모리     │ 8GB / 사용중 3.2GB (40%)            │
│ 디스크     │ 100GB / 사용중 45GB (45%)           │
│ 가동시간   │ 15일 4시간                          │
├────────────┴────────────────────────────────────┤
│  실행 중인 서비스                                 │
│  ● nginx.service        (포트 80, 443)           │
│  ● app.service          (포트 8000)              │
│  ● postgresql.service   (포트 5432)              │
│  ● docker               (컨테이너 3개 실행 중)    │
├─────────────────────────────────────────────────┤
│  [모니터링 설치 시작]                             │
└─────────────────────────────────────────────────┘
```

---

## STEP 3. 모니터링 에이전트 설치

### 3-1. 설치 방법 선택

```
┌─────────────────────────────────────────┐
│  모니터링 설치 방법 선택                  │
│                                         │
│  ● Ansible 자동 설치 (추천)              │
│  ○ 수동 설치 (SSH 직접 접속)             │
│  ○ Docker Compose 방식                  │
│                                         │
│  모니터링 대상:                          │
│  [✅] CPU 사용률                         │
│  [✅] 메모리 사용률                      │
│  [✅] 디스크 I/O                        │
│  [✅] 네트워크 트래픽                    │
│  [✅] 애플리케이션 응답시간              │
│                                         │
│         [설치 시작]                      │
└─────────────────────────────────────────┘
```

### 3-2. Ansible 자동 설치

```yaml
# playbooks/02_monitoring.yml

- name: 모니터링 에이전트 설치
  hosts: target_server
  become: yes

  tasks:
    # CloudWatch Agent 설치
    - name: CloudWatch Agent 다운로드
      get_url:
        url: https://s3.amazonaws.com/amazoncloudwatch-agent/ubuntu/amd64/latest/amazon-cloudwatch-agent.deb
        dest: /tmp/cloudwatch-agent.deb

    - name: CloudWatch Agent 설치
      apt:
        deb: /tmp/cloudwatch-agent.deb

    - name: CloudWatch Agent 설정 파일 배포
      template:
        src: cloudwatch-config.json.j2
        dest: /opt/aws/amazon-cloudwatch-agent/etc/amazon-cloudwatch-agent.json

    - name: CloudWatch Agent 시작
      service:
        name: amazon-cloudwatch-agent
        state: started
        enabled: yes

    # Prometheus Node Exporter 설치 (Grafana 연동용)
    - name: Node Exporter 설치
      get_url:
        url: https://github.com/prometheus/node_exporter/releases/download/v1.7.0/node_exporter-1.7.0.linux-amd64.tar.gz
        dest: /tmp/node_exporter.tar.gz

    - name: Node Exporter 압축 해제
      unarchive:
        src: /tmp/node_exporter.tar.gz
        dest: /opt/
        remote_src: yes

    - name: Node Exporter 서비스 등록
      copy:
        content: |
          [Unit]
          Description=Prometheus Node Exporter
          [Service]
          ExecStart=/opt/node_exporter-1.7.0.linux-amd64/node_exporter
          [Install]
          WantedBy=multi-user.target
        dest: /etc/systemd/system/node_exporter.service

    - name: Node Exporter 시작
      service:
        name: node_exporter
        state: started
        enabled: yes

    # 방화벽 포트 오픈
    - name: 모니터링 포트 오픈
      ufw:
        rule: allow
        port: "{{ item }}"
      loop:
        - "9100"   # Node Exporter
        - "443"    # CloudWatch (HTTPS)
```

### 3-3. 수동 설치 방법 (SSH 직접)

```bash
# 1. CloudWatch Agent 설치
wget https://s3.amazonaws.com/amazoncloudwatch-agent/ubuntu/amd64/latest/amazon-cloudwatch-agent.deb
sudo dpkg -i amazon-cloudwatch-agent.deb

# 2. 설정 파일 생성
sudo /opt/aws/amazon-cloudwatch-agent/bin/amazon-cloudwatch-agent-config-wizard
# → 질문에 답변:
#   On-Premises? → yes
#   CPU 메트릭 수집? → yes
#   메모리 메트릭 수집? → yes
#   로그 수집? → 필요 시 yes

# 3. IAM 자격증명 설정 (CloudWatch 전송용)
sudo mkdir -p /root/.aws
sudo cat > /root/.aws/credentials << EOF
[AmazonCloudWatchAgent]
aws_access_key_id = 발급받은_ACCESS_KEY
aws_secret_access_key = 발급받은_SECRET_KEY
EOF

# 4. 시작
sudo /opt/aws/amazon-cloudwatch-agent/bin/amazon-cloudwatch-agent-ctl \
    -a fetch-config \
    -m onPremise \
    -s \
    -c file:/opt/aws/amazon-cloudwatch-agent/etc/amazon-cloudwatch-agent.json
```

### 다른 방법: Docker로 모니터링 스택 설치

```yaml
# docker-compose.monitoring.yml
services:
  node-exporter:
    image: prom/node-exporter:latest
    ports:
      - "9100:9100"
    volumes:
      - /proc:/host/proc:ro
      - /sys:/host/sys:ro
    command:
      - '--path.procfs=/host/proc'
      - '--path.sysfs=/host/sys'
    restart: unless-stopped
```

```bash
docker compose -f docker-compose.monitoring.yml up -d
```

---

## STEP 4. VPN 터널 구성

### 방법 A: WireGuard (직접 운영)

#### AWS EC2 WireGuard 서버 설정

```
AWS 콘솔에서:
1. EC2 → 인스턴스 시작
   - AMI: Ubuntu 24.04
   - 인스턴스 유형: t3.micro
   - 이름: wireguard-server

2. 보안 그룹 설정:
   - 인바운드: UDP 51820 (WireGuard)
   - 인바운드: TCP 22 (SSH)
   
3. 탄력적 IP 할당:
   EC2 → 탄력적 IP → 할당 → 인스턴스에 연결
```

```bash
# EC2 WireGuard 서버 설치
sudo apt update && sudo apt install -y wireguard

# 키 쌍 생성
wg genkey | sudo tee /etc/wireguard/server_private.key
sudo cat /etc/wireguard/server_private.key | wg pubkey | sudo tee /etc/wireguard/server_public.key

# 서버 설정
sudo cat > /etc/wireguard/wg0.conf << EOF
[Interface]
PrivateKey = $(sudo cat /etc/wireguard/server_private.key)
Address = 10.200.0.1/24
ListenPort = 51820
PostUp = iptables -A FORWARD -i wg0 -j ACCEPT; iptables -t nat -A POSTROUTING -o eth0 -j MASQUERADE
PostDown = iptables -D FORWARD -i wg0 -j ACCEPT; iptables -t nat -D POSTROUTING -o eth0 -j MASQUERADE

# VM2 (API 서버)
[Peer]
PublicKey = VM2_PUBLIC_KEY_여기에
AllowedIPs = 10.200.0.2/32

# VM3 (DB 서버)
[Peer]
PublicKey = VM3_PUBLIC_KEY_여기에
AllowedIPs = 10.200.0.3/32
EOF

sudo wg-quick up wg0
sudo systemctl enable wg-quick@wg0
```

```bash
# VM2, VM3에서 WireGuard 클라이언트 설치
sudo apt install -y wireguard

wg genkey | sudo tee /etc/wireguard/private.key
sudo cat /etc/wireguard/private.key | wg pubkey | sudo tee /etc/wireguard/public.key

sudo cat > /etc/wireguard/wg0.conf << EOF
[Interface]
PrivateKey = $(sudo cat /etc/wireguard/private.key)
Address = 10.200.0.2/32   # VM3는 10.200.0.3

[Peer]
PublicKey = EC2_SERVER_PUBLIC_KEY
Endpoint = EC2_공인IP:51820
AllowedIPs = 10.200.0.0/24
PersistentKeepalive = 25
EOF

sudo wg-quick up wg0
```

```yaml
# Ansible로 자동화
# playbooks/03_wireguard.yml
- name: WireGuard VPN 클라이언트 설치
  hosts: onpremise_servers
  become: yes
  tasks:
    - name: WireGuard 설치
      apt:
        name: wireguard
        state: present

    - name: 키 쌍 생성
      shell: wg genkey | tee /etc/wireguard/private.key | wg pubkey > /etc/wireguard/public.key
      args:
        creates: /etc/wireguard/private.key

    - name: WireGuard 설정 배포
      template:
        src: wg0-client.conf.j2
        dest: /etc/wireguard/wg0.conf
        mode: '0600'

    - name: WireGuard 시작
      service:
        name: wg-quick@wg0
        state: started
        enabled: yes
```

---

### 방법 B: Tailscale (관리형, 추천)

#### 설치 방법 비교

| 방법 | 난이도 | 특징 |
|---|---|---|
| 스크립트 설치 | 쉬움 | 한 줄 명령 |
| 패키지 매니저 | 중간 | apt/yum |
| Docker | 중간 | 컨테이너로 실행 |
| Ansible | 자동 | 다수 서버 동시 설치 |

```bash
# VM2, VM3에서 Tailscale 스크립트 설치 (가장 간단)
curl -fsSL https://tailscale.com/install.sh | sh
sudo tailscale up --authkey=tskey-발급받은키

# AWS EC2에서도 동일하게
curl -fsSL https://tailscale.com/install.sh | sh
sudo tailscale up --authkey=tskey-발급받은키

# 연결 확인
tailscale status
# → 모든 노드가 100.x.x.x 대역으로 연결됨
```

```yaml
# Ansible로 자동화
# playbooks/03_tailscale.yml
- name: Tailscale VPN 설치
  hosts: all
  become: yes
  vars:
    tailscale_authkey: "{{ vault_tailscale_key }}"
  tasks:
    - name: Tailscale 설치 스크립트 실행
      shell: curl -fsSL https://tailscale.com/install.sh | sh
      args:
        creates: /usr/bin/tailscale

    - name: Tailscale 연결
      shell: tailscale up --authkey={{ tailscale_authkey }}
      register: result

    - name: 연결 상태 확인
      shell: tailscale status
      register: status
    
    - name: 상태 출력
      debug:
        msg: "{{ status.stdout }}"
```

```
Tailscale 관리 콘솔 (admin.tailscale.com) 에서:
1. Machines 탭 → 연결된 노드 목록 확인
2. ACL 설정 → EC2가 VM3 100.x.x.x:5432 접근 허용
3. 서브넷 라우팅 → VM3가 있는 대역 전체 노출 가능
```

---

## STEP 5. 스테이징 환경 구성 (운영 서버 복제)

### 왜 스테이징이 필요한가

```
운영 서버에 직접 부하테스트 → 실제 서비스 장애 발생 가능
스테이징 서버 = 운영과 동일한 환경의 테스트 전용 서버
```

### 방법 A: 동일 서버에 포트 분리

```bash
# 운영: 포트 8000
# 스테이징: 포트 8001 (같은 서버, 다른 포트)

docker run -d \
  --name stays-staging \
  -p 8001:8000 \
  -e DATABASE_URL=postgresql://postgres:pw@localhost:5433/stays_staging \
  dk12dl/stays-backend:latest
```

```
장점: 별도 서버 불필요
단점: 운영 서버 자원 공유 → 부하 영향 있음
```

### 방법 B: AWS EC2에 스테이징 구성 (추천)

```
1. EC2 인스턴스 생성 (t3.medium)
2. ECR에서 이미지 pull
3. 온프레미스 DB 스냅샷 → 스테이징 DB 생성
4. 스테이징 서버에 JMeter 부하
5. CPU 급등 → AWS 자동 확장 테스트
```

```bash
# EC2에서 실행
aws ecr get-login-password --region ap-northeast-2 | \
  docker login --username AWS --password-stdin 계정ID.dkr.ecr.ap-northeast-2.amazonaws.com

docker run -d \
  --name stays-staging \
  -p 8000:8000 \
  -e DATABASE_URL=postgresql://postgres:pw@100.x.x.VM3:5432/stays_db \
  계정ID.dkr.ecr.ap-northeast-2.amazonaws.com/stays-backend:latest
```

### 방법 C: Ansible로 스테이징 자동 구성

```yaml
# playbooks/04_staging.yml
- name: 스테이징 환경 구성
  hosts: staging_server
  become: yes
  tasks:
    - name: Docker 설치
      shell: curl -fsSL https://get.docker.com | sh

    - name: ECR 로그인
      shell: |
        aws ecr get-login-password --region ap-northeast-2 | \
        docker login --username AWS --password-stdin {{ ecr_url }}

    - name: 최신 이미지 pull
      docker_image:
        name: "{{ ecr_url }}/stays-backend:latest"
        source: pull

    - name: 스테이징 컨테이너 시작
      docker_container:
        name: stays-staging
        image: "{{ ecr_url }}/stays-backend:latest"
        ports:
          - "8000:8000"
        env:
          DATABASE_URL: "{{ staging_db_url }}"
        state: started
        restart_policy: unless-stopped
```

---

## STEP 6. 부하테스트 실행 위치

### 옵션 비교

| 위치 | 장점 | 단점 |
|---|---|---|
| VM4 (로컬) | 네트워크 지연 없음 | 학원 내 제한 |
| AWS EC2 JMeter | 대규모 부하, 외부 트래픽 시뮬레이션 | EC2 비용 |
| BlazeMeter | 클라우드 부하, 보고서 자동화 | 유료 |

### AWS EC2에서 JMeter 실행 (추천)

```
AWS 콘솔:
1. EC2 → 인스턴스 시작
   - AMI: Ubuntu 24.04
   - 인스턴스 유형: t3.large (JMeter 메모리 필요)
   - 이름: jmeter-runner

2. 보안 그룹:
   - SSH 22 허용
   - 필요 시 22222 (JMeter 분산 실행)
```

```bash
# EC2 JMeter 서버에서
sudo apt update && sudo apt install -y openjdk-17-jdk wget

wget https://downloads.apache.org/jmeter/binaries/apache-jmeter-5.6.3.tgz
tar -xzf apache-jmeter-5.6.3.tgz
sudo mv apache-jmeter-5.6.3 /opt/jmeter
sudo ln -s /opt/jmeter/bin/jmeter /usr/local/bin/jmeter

# JMX 파일 업로드 (로컬에서)
scp -i key.pem loadtest/jmeter/scenario1_coupon_rush.jmx ubuntu@EC2_IP:~/

# JMeter 실행
jmeter -n \
  -t ~/scenario1_coupon_rush.jmx \
  -JBASE_URL=http://스테이징서버IP \
  -JAPI_PORT=8000 \
  -l ~/results/scenario1.jtl \
  -e -o ~/results/scenario1_report
```

---

## STEP 7. CloudWatch 경보 → 자동 확장

### AWS 콘솔에서 설정

```
CloudWatch 설정:
1. CloudWatch → 지표 → 사용자 지정 네임스페이스 확인
   → CWAgent/cpu_usage_active 지표 존재 확인

2. CloudWatch → 경보 → 경보 생성
   → 지표 선택: CWAgent > cpu_usage_active
   → 조건: 80% 초과 후 2분 지속
   → 작업: SNS 주제에 알림 전송
   → SNS → Lambda 트리거 연결

3. Lambda 함수 생성
   → 런타임: Python 3.12
   → 역할: EC2 생성 + ALB 등록 권한
```

```python
# Lambda 함수 코드
import boto3

def lambda_handler(event, context):
    ec2 = boto3.client('ec2', region_name='ap-northeast-2')
    elbv2 = boto3.client('elbv2', region_name='ap-northeast-2')

    # EC2 인스턴스 생성
    response = ec2.run_instances(
        ImageId='ami-ubuntu-24-04',
        InstanceType='t3.medium',
        MinCount=1,
        MaxCount=1,
        UserData="""#!/bin/bash
            curl -fsSL https://get.docker.com | sh
            aws ecr get-login-password --region ap-northeast-2 | \
              docker login --username AWS --password-stdin ECR_URL
            docker run -d -p 8000:8000 \
              -e DATABASE_URL=postgresql://postgres:pw@DB_IP:5432/stays_db \
              ECR_URL/stays-backend:latest
        """,
        TagSpecifications=[{
            'ResourceType': 'instance',
            'Tags': [{'Key': 'Name', 'Value': 'stays-api-scaled'}]
        }]
    )

    instance_id = response['Instances'][0]['InstanceId']

    # ALB 타겟 그룹에 등록 (EC2 부팅 후)
    import time
    time.sleep(60)

    elbv2.register_targets(
        TargetGroupArn='arn:aws:elasticloadbalancing:...',
        Targets=[{'Id': instance_id, 'Port': 8000}]
    )

    return {'statusCode': 200, 'body': f'EC2 {instance_id} 생성 및 ALB 등록 완료'}
```

---

## STEP 8. Ansible 전체 자동화 구조

### Playbook 폴더 구조

```
ansible/
├── inventory/
│   ├── hosts.yml              # 서버 목록
│   └── group_vars/
│       ├── all.yml            # 공통 변수
│       └── vault.yml          # 암호화된 민감 정보
├── playbooks/
│   ├── 01_collect_info.yml    # 서버 정보 수집
│   ├── 02_monitoring.yml      # CloudWatch + Node Exporter
│   ├── 03_vpn.yml             # WireGuard or Tailscale
│   ├── 04_staging.yml         # 스테이징 환경 구성
│   ├── 05_loadtest.yml        # 부하테스트 실행
│   └── 06_cleanup.yml         # 테스트 후 정리
├── roles/
│   ├── common/                # 공통 패키지
│   ├── cloudwatch/            # CloudWatch Agent
│   ├── wireguard/             # WireGuard VPN
│   ├── tailscale/             # Tailscale VPN
│   └── docker/                # Docker 설치
└── site.yml                   # 전체 실행 엔트리포인트
```

### 실행 예시

```bash
# 전체 자동 설정
ansible-playbook -i inventory/hosts.yml site.yml

# 단계별 실행
ansible-playbook -i inventory/hosts.yml playbooks/02_monitoring.yml
ansible-playbook -i inventory/hosts.yml playbooks/03_vpn.yml --tags tailscale

# 특정 서버만
ansible-playbook -i inventory/hosts.yml playbooks/02_monitoring.yml \
  --limit api_server
```

---

## 미결정 사항

1. **VPN 방식**: WireGuard (직접 운영) vs Tailscale (관리형)
2. **스테이징 위치**: 온프레미스 포트 분리 vs AWS EC2
3. **부하테스트 서버**: VM4 로컬 vs AWS EC2 JMeter
4. **자동 회수 조건**: CPU 몇 % 이하로 몇 분 유지되면 EC2 종료할지
5. **웹폼 구현**: 별도 서비스로 만들 것인지, 관리자 콘솔로 만들 것인지
6. **Ansible 적용 범위**: 전체 자동화할지, 일부 수동으로 할지
