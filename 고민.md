# AWS 자동 확장 연동 아키텍처 고민 정리

## 프로젝트 목표

온프레미스 서버(VMware VM)에서 부하테스트로 CPU 80% 초과 발생 시
AWS 자원을 자동으로 투입해 트래픽을 분산하고, 정상화되면 회수하는 구조 시연.

---

## 확장 대상 결정

### 결론: App + API 서버만 확장, DB는 온프레미스 유지

| 서버 | 확장 여부 | 이유 |
|---|---|---|
| VM1 앱 서버 (Nginx+React) | 보류 | 정적 파일 서빙이라 부하 거의 없음. 확장 의미 크지 않음 |
| VM2 API 서버 (FastAPI) | ✅ 확장 | 쿠폰/예약 DB 락으로 CPU 급등 원인. 핵심 확장 대상 |
| VM3 DB 서버 (PostgreSQL) | ❌ 유지 | DB를 AWS에 복제하면 복잡도 폭발. 온프레미스 단일 DB 유지 |

### 고민 포인트
- App 서버도 확장할지 미결정. 현재는 API만 확장하는 방향.
- DB를 온프레미스에 두면 AWS EC2 API 서버들이 VPN 통해 DB 접근해야 함 → 네트워크 연결 필수

---

## 네트워크 연결 방식 고민

### 환경 제약
- 학원 Wi-Fi 사용 중
- 라우터 설정 접근 불가
- 고정 공인 IP 없음

### 검토한 옵션들

#### 1. Site-to-Site VPN ❌
```
온프레미스 라우터 ←── IPSec ──→ AWS Virtual Private Gateway
```
- AWS 공식 솔루션, 안정적
- 비용: $0.05/시간 ($36/월) → 테스트 목적이면 몇 시간만 켜도 $0.1 수준으로 저렴
- **문제: 학원 라우터 설정 필요 → 불가능**

#### 2. Direct Connect ❌
```
온프레미스 데이터센터 ←── 광케이블 (물리 전용선) ──→ AWS
```
- 기업용 전용선 솔루션
- 설치 수주~수개월, 비용 수백만원/월
- **물리적으로 학원 환경에서 불가능**

#### 3. WireGuard ✅ 검토 중
```
VM2 ──┐
VM3 ──┼── UDP 51820 ──→ EC2 WireGuard 서버 (공인 IP)
      └──────────────→       │
                        (VPN 허브 역할)
                        EC2 API들이 VM3 DB 접근 가능
```
- 학원 NAT 환경에서 작동 (VM에서 아웃바운드로 EC2에 먼저 연결)
- WireGuard 서버용 EC2 t3.micro 추가 필요 ($8~10/월)
- 직접 설정: 키 생성, 피어 등록, 라우팅 설정
- 완전한 제어권, 외부 서비스 의존 없음

**구조:**
```
VM2, VM3 → WireGuard 클라이언트 설치
EC2 WireGuard 서버 → 피어로 등록
EC2 API들 → 10.0.0.x 대역으로 VM3 DB 접근
```

#### 4. Tailscale ✅ 검토 중
```
VM2 ──┐
VM3 ──┼── HTTPS ──→ Tailscale 코디네이터 서버
EC2 ──┘             (자동 메시 P2P 연결)
                    각 노드에 100.x.x.x 가상 IP 부여
```
- 설치 후 로그인만 하면 자동으로 메시 네트워크 형성
- 별도 EC2 서버 불필요
- 무료 플랜 20대 이하
- WireGuard 프로토콜 기반이라 성능 동일
- 단점: Tailscale 서비스에 의존

**구조:**
```
VM2, VM3, EC2들 모두 Tailscale 설치 + 같은 계정 로그인
자동으로 100.x.x.x 가상 IP 부여
EC2 API → 100.x.x.VM3_IP 로 DB 접근
```

### WireGuard vs Tailscale 비교

| 항목 | WireGuard | Tailscale |
|---|---|---|
| 추가 EC2 필요 | ✅ t3.micro ($8~10/월) | ❌ 불필요 |
| 설정 난이도 | 중간 (키 생성, 피어 등록) | 쉬움 (설치+로그인) |
| 외부 의존성 | 없음 | Tailscale 서비스 |
| 제어권 | 완전 직접 관리 | Tailscale 위임 |
| 성능 | 빠름 | 동일 (WireGuard 기반) |
| 비용 | EC2 추가 비용 | 무료 (20대 이하) |

### 미결정
- WireGuard vs Tailscale 최종 선택 아직 안 함
- 데모 목적이면 Tailscale이 설정 빠르고 무료
- 제어권/학습 목적이면 WireGuard가 더 의미있음

---

## 전체 아키텍처 (확정된 부분)

### 트래픽 분산: ALB 사용 (Route53 없음)

```
평상시
─────────────────────────────────────────────
VM4 (JMeter) ──→ VM2 (API 서버, 온프레미스)
                      │
                   VM3 (DB)

CPU 80% 초과 이후
─────────────────────────────────────────────
VM4 (JMeter) ──→ ALB ──→ EC2 API #1
                    └──→ EC2 API #2
                               │
                         VPN 터널 (WireGuard or Tailscale)
                               │
                          VM3 DB (온프레미스)
```

### AWS 서비스 구성

| 서비스 | 역할 |
|---|---|
| ECR | stays-backend 도커 이미지 저장 |
| EC2 (API) | stays-backend 실행, t3.medium (VM2와 동일 사양) |
| EC2 (WireGuard) | VPN 허브 - WireGuard 선택 시만 | 
| ALB | EC2들 트래픽 분산 |
| CloudWatch | VM2 CPU 메트릭 수집 + 경보 설정 |
| Lambda | EC2 생성 + ALB 타겟 자동 등록 |
| S3 | docker-compose, 설정 파일 저장 |
| Prometheus + Grafana | 실시간 모니터링 (별도 구성) |

### 자동 확장 트리거 흐름

```
1. JMeter 부하 시작 (VM4 → VM2)
2. VM2 CPU 상승
3. CloudWatch Agent (VM2에 설치) → CPU 메트릭 AWS로 전송
4. CloudWatch 경보: CPU > 80% 지속 → 알람 발동
5. Lambda 실행:
   a. ECR에서 stays-backend 이미지 pull
   b. EC2 인스턴스 생성 (t3.medium)
   c. 컨테이너 실행 (DB 접속: WireGuard/Tailscale 터널 통해 VM3)
   d. ALB 타겟 그룹에 새 EC2 등록
6. ALB가 VM2 + EC2들로 트래픽 분산
7. VM2 CPU 정상화 → 일정 시간 후 EC2 종료 (회수)
```

---

## EC2 사양 결정

VM2 기준으로 동일하게 맞춤:

| 옵션 | 스펙 | 비용 | 결론 |
|---|---|---|---|
| t3.small | 2vCPU / 2GB | ~$15/월 | 최소 사양 |
| **t3.medium** | **2vCPU / 4GB** | **~$30/월** | **추천 (VM2와 동일)** |
| t3.large | 2vCPU / 8GB | ~$60/월 | 과사양 |

- 테스트 목적이라 사용 시간만큼만 과금
- 몇 시간 테스트 = 몇 달러 수준

---

## 아직 결정 안 된 것

1. **WireGuard vs Tailscale** 최종 선택
2. **App 서버(VM1) 확장 여부** (현재 API만 확장 방향)
3. **Lambda 자동화 범위** (EC2 생성만? Auto Scaling Group 사용?)
4. **정상화 기준** (CPU 몇 % 이하로 몇 분 유지되면 EC2 회수?)
5. **모니터링 구성** (Prometheus + Grafana 별도 VM? EC2?)
6. **시나리오 2, 4 테마** (아직 미결정)

---

## 다음 할 일

1. WireGuard vs Tailscale 결정
2. AWS 계정에 VPC, ALB, ECR, CloudWatch 세팅
3. VM2에 CloudWatch Agent 설치 → CPU 메트릭 전송
4. Lambda 함수 작성 (EC2 생성 + ALB 등록)
5. VPN 구성 (결정된 방식으로)
6. 전체 흐름 테스트
