# 프로젝트 정리

---

## 하려는 것 (최종 목표)

**온프레미스 서버가 트래픽 폭주로 CPU 임계치(80%)를 초과하면
AWS 자원을 자동으로 투입해 트래픽을 분산하고,
정상화되면 자원을 회수하는 시스템을 시연.**

시연용 서비스로 **솔데스크 숙박예약 웹사이트**를 만들었음.
JMeter로 1000명 동시 접속 → 쿠폰 발급/예약 생성 시 DB 락 경합 → CPU 급등 → AWS 자동 확장.

---

## 전체 아키텍처

### 온프레미스 (VMware / UTM)
```
VM1  앱 서버    Nginx + React          :80
VM2  API 서버   FastAPI + asyncpg      :8000
VM3  DB 서버    PostgreSQL 16          :5432
VM4  부하 서버  JMeter + Prometheus + Grafana
```

### AWS (예정)
```
ECR           Docker 이미지 저장
EC2 (API)     stays-backend 실행 (t3.medium)
ALB           온프레미스 + AWS EC2 트래픽 분산
CloudWatch    VM2 CPU 메트릭 수집 + 경보
Lambda        EC2 자동 생성 + ALB 등록
Tailscale     온프레미스 ↔ AWS VPN 터널
```

---

## 기술 스택

| 레이어 | 기술 |
|---|---|
| 프론트엔드 | React + Vite + shadcn/ui + Leaflet(지도) |
| 백엔드 | FastAPI (Python) + asyncpg |
| DB | PostgreSQL 16 |
| 인증 | JWT Bearer Token |
| 배포 | Docker + Docker Compose |
| 이미지 저장소 | Docker Hub (soldesk1184) |
| 부하테스트 | JMeter + BlazeMeter |
| 모니터링 | Prometheus + Grafana |
| VPN | Tailscale (예정) |

---

## 지금까지 완료한 것

### 백엔드 (FastAPI)
- 전체 API 구현 완료
  - POST /auth/signup, /auth/login
  - GET/POST /stays, /stays/:id, /stays/:id/rooms, /stays/:id/reviews
  - GET /events, /events/:id
  - POST /coupons/issue (SELECT FOR UPDATE 비관적 락 — CPU 급등 핵심)
  - GET /coupons/me, POST /coupons/validate
  - POST /bookings, GET /bookings/me, GET /bookings/:id
- CORS 전체 허용 (allow_origins=["*"])
- bcrypt 버전 고정 (passlib 호환)

### 프론트엔드 (React)
- 전 페이지 백엔드 API 연동 완료
  - 홈(Index), 숙소목록(Stays), 이벤트(Events), 이벤트상세(EventDetail)
  - 숙소상세(StayDetail) — 실제 DB 데이터, 리뷰, 지도
  - 예약(Booking), 예약완료(BookingComplete), 마이페이지(MyPage), 쿠폰(Coupons)
- KakaoMap → Leaflet(OpenStreetMap)으로 교체 (API 키 불필요)
- 솔데스크 3개 지점 지도 마커 + 클릭 시 상세페이지 이동

### DB (PostgreSQL)
- 솔데스크 실제 데이터 기반 schema.sql 완성
  - 숙소 121개 (서울/제주/부산/완도/광주)
  - 솔데스크 3개 지점 (종로점, 강남파빌리온점, 강남성옥빌딩점)
  - 이벤트: 솔데스크 전 지점 동시 오픈 기념 (30% 할인)
  - 쿠폰: SOLD-0001~0500 + SOLD-LOAD (부하테스트 풀 1000장)
  - 리뷰 10,562개, 예약 3,000개, 유저 106명
  - 어드민 계정: admin@stays.com / admin1234

### Docker 이미지 (Docker Hub)
| 이미지 | 플랫폼 | 용도 |
|---|---|---|
| soldesk1184/stays-backend:latest | linux/amd64 | Windows VMware |
| soldesk1184/stays-frontend:latest | linux/amd64 | Windows VMware |
| soldesk1184/mac-stays-backend:latest | linux/arm64 | Mac M3 UTM |
| soldesk1184/mac-stays-frontend:latest | linux/arm64 | Mac M3 UTM |

### 배포 스크립트
```
deploy/
├── build-push.sh           Windows 이미지 빌드 (amd64)
├── build-push-arm64.sh     Mac M3 이미지 빌드 (arm64)
├── vm1-app ~ vm4-loadtest/ Windows VMware용
└── mac-vm1-app ~ mac-vm4-loadtest/ Mac M3 UTM용
```

### 부하테스트 시나리오
| 번호 | 내용 | 유저 | Ramp-up |
|---|---|---|---|
| S1 | 쿠폰 선착순 폭발 (SOLD-LOAD) | 1000명 | 10초 |
| S3 | 이벤트 오픈 복합 플로우 | 800명 | 30초 |
| S2 | 특가 숙소 예약 경쟁 | 500명 | 5초 (미정) |
| S4 | 인기 숙소 조회 폭주 | 2000명 | 60초 (미정) |

### 모니터링
- Node Exporter: VM1, VM2, VM3 (포트 9100)
- postgres_exporter: VM3 (포트 9187)
- Prometheus + Grafana: VM4 (포트 9090, 3000)
- Grafana 대시보드: 1860 (Node Exporter), 9628 (PostgreSQL)

---

## 현재 진행 중인 것

### Windows VMware 환경 (10.0.2.x)
- VM1: 10.0.2.139 — 앱 서버 운영 중
- VM2: 10.0.2.138 — API 서버 운영 중
- VM3: 10.0.2.137 — DB 서버 운영 중
- VM4: 10.0.2.136 — 부하 서버 준비 중

현재 상태:
- 사이트 접속 가능 (http://10.0.2.139)
- DB 연결 확인 완료
- 부하테스트 실행 전 단계

### Mac M3 UTM 환경 (192.168.64.x)
- Ubuntu Server ARM64 4개 VM 설치 중
- IP 대역: 192.168.64.2~5 (예정)
- 이미지 빌드 필요 (build-push-arm64.sh)

---

## 앞으로 해야 할 것

### 단기 (지금 당장)
1. **부하테스트 실행** (Windows VM4)
   ```bash
   python3 ~/loadtest/scripts/create_users.py --url http://10.0.2.138:8000 --count 2000
   ./run.sh 1 10.0.2.138   # 시나리오1: 쿠폰 선착순
   ./run.sh 3 10.0.2.138   # 시나리오3: 복합 플로우
   ```
2. **Grafana 모니터링 확인** — VM2 CPU 80% 급등 확인
3. **Mac M3 UTM VM 세팅 완료**
4. **솔데스크 숙소 이미지 업로드** (images/ 폴더에 3개 파일)

### 중기
5. **시나리오 2, 4 테마 결정 및 구현**
6. **AWS 연동**
   - Tailscale VPN (온프레미스 ↔ AWS)
   - ECR에 이미지 등록
   - CloudWatch Agent VM2에 설치
   - Lambda 함수 작성 (EC2 자동 생성 + ALB 등록)
   - ALB 설정

### 장기
7. **BlazeMeter 연동** (대규모 클라우드 부하테스트)
8. **Admin 페이지 API 연동** (현재 mock 데이터)
9. **AWS 자동 회수 로직** (CPU 정상화 시 EC2 종료)

---

## 주요 파일 위치

| 파일 | 경로 |
|---|---|
| 백엔드 API | backend/app/routers/ |
| DB 스키마 | deploy/vm3-db/schema.sql |
| JMeter 시나리오 | loadtest/jmeter/ |
| 부하테스트 문서 | loadtest/부하테스트_시나리오_문서.md |
| AWS 아키텍처 고민 | 고민.md |
| 온프레미스 클라우드 설계 | 온프레미스_클라우드확장_설계.md |
| Mac UTM 설치 가이드 | 맥M3_UTM_설치가이드.md |
| 다음 작업 프롬프트 | 다음_작업_프롬프트.md |

---

## 계정 정보

| 구분 | 값 |
|---|---|
| 어드민 | admin@stays.com / admin1234 |
| 테스트 유저 | user1@test.com / password123 |
| Docker Hub | soldesk1184 |
| GitHub | Incheon-soda/scaling-service |
| DB | postgres / stays_password / stays_db |
| Grafana | admin / admin1234 |
